{
  "name": "betterloop",
  "version": "1.0.0",
  "description": "WebMCP continuity layer that helps Codex verify and continue long-running tasks",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "npm run build && npx tsc --noEmit && node scripts/final-check.cjs && node scripts/betterloop-stop.cjs --self-test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
