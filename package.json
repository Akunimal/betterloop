{
  "name": "mcpation",
  "version": "1.0.0",
  "description": "A browser-native WebMCP Codex Environment Doctor for workspace readiness, hardening, and verification",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types scripts/mcpation-tests.ts",
    "verify": "npm run build && npx tsc --noEmit && npm test && node scripts/final-check.cjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
