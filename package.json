{
  "name": "mcpation",
  "version": "1.0.0",
  "description": "A consent-first WebMCP environment doctor for local MCP configurations",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "companion": "node scripts/mcpation-companion.cjs",
    "verify": "npm run build && npx tsc --noEmit && node scripts/final-check.cjs && node scripts/mcpation-companion.cjs --self-test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
